Transactions 命令

1. DISCARD
    刷新一个事务中所有在排队等待的指令，并且将连接状态恢复到正常。

2. WATCH key [key ...]
    标记所有指定的key别监视起来，在事务中有条件的执行（乐观锁）

3. EXEC
    执行事务中所有在排队等待的指令并将连接状态恢复到正常

4. MULTI
    标记一个事务块的开始。随后指令将在执行EXEC时，作为一个原子执行。

5. UNWATCH
    刷新一个事务中已被监视的所有的key
